const HINTS: Readonly<Record<number, readonly [string, string, string]>> = {
  1: ["先找到输出文字所用的 Python 内置函数。", "函数名后需要一对圆括号，文字放在引号中。", "检查中文冒号和引号是否误写进代码语法。"],
  2: ["这道题只需要一次输出，不需要变量。", "注意 Python 区分英文逗号、括号和引号。", "逐字核对大小写、逗号和感叹号。"],
  3: ["先把名字保存到变量，再输出变量本身。", "赋值使用一个等号，变量名不要放在引号里。", "输出时传入 hero_name，而不是再次写名字文本。"],
  4: ["先保留题目给出的两个价格变量。", "总价可以由两个变量使用加法运算得到。", "最终只输出计算结果，不要附加说明文字。"],
  5: ["函数需要覆盖 boss_hp 大于零和不大于零两种情况。", "使用 if/else，并在两个分支中返回字符串。", "边界值 0 应与负数得到相同结果。"],
  6: ["先创建空列表，再逐个加入回合编号。", "range 的结束值不会被包含，需要为上界多留一步。", "total_turns 为 0 时循环应自然跳过。"],
  7: ["每轮把当前 start 加入列表，然后让它变小。", "循环条件只应允许正数继续执行。", "别忘了在循环体内更新 start，否则会超时。"],
  8: ["伤害来自攻击减防御，但不能低于 1。", "可以使用 max 在计算值和 1 之间取较大者。", "函数返回伤害后，再按题目要求输出调用结果。"],
  9: ["这个函数只依赖参数 hp，不需要全局变量。", "正数与零的分界条件是 hp > 0。", "两个分支都要用 return，而不是 print。"],
  10: ["准备一个新列表，按原列表顺序逐项检查。", "只有新列表中还没有当前物品时才追加。", "不要排序、转成 set 或修改传入的 items。"],
  11: ["使用键名读取字典，不要依赖插入顺序。", "HP 是整数，拼接前可以用格式化字符串转换。", "返回值中名字和 HP 之间只有一个空格。"],
  12: ["先处理首尾空白，再处理两类标点。", "连续感叹号可能超过两个，可以循环替换直到稳定。", "三个英文点号为一组，替换成一个中文句号。"],
  13: ["分别移除开始标签和结束标签。", "字符串 replace 可以连续调用。", "最终输出只保留标签内部的中文内容。"],
  14: ["pathlib.Path 已经知道如何拆分文件名。", "读取 Path 对象的 suffix 属性。", "无扩展名时 suffix 会自然得到空字符串。"],
  15: ["把可能除零的表达式放进 try。", "只捕获 ZeroDivisionError，不要吞掉所有异常。", "正常路径直接返回商，异常路径返回指定字符串。"],
  16: ["从 values 中逐项挑选满足条件的数字。", "列表推导式由表达式、for 和 if 三部分组成。", "保留原顺序，只筛选严格大于 100 的值。"],
  17: ["先让 dataclass 装饰器作用在 Monster 类上。", "三个字段都要写名称和准确的类型提示。", "伤害至少为 1；更新 HP 时再用一次 max 防止负数。"],
};

export const getChapterHints = (chapterNumber: number): readonly string[] => {
  const hints = HINTS[chapterNumber];
  if (!hints) throw new RangeError(`Unknown chapter hints: ${chapterNumber}`);
  return hints;
};
