# MVP 与本地完整功能验证记录（更新于 2026-07-17）

## 自动门禁

- `npm test`：68/68 通过；覆盖公开契约、AI 流式合同/上下文校验/双模式与第 50 次成功回应称号、17 章叙事密度与消息合同、消息播放节奏、玩家/NPC 归类与误判回归、存档迁移、背包/药水、16 个称号、10 套 Boss 对话、私有评测聚合、分享卡公开字段、奖励幂等性，以及双主题预定成品资产完整性。
- `npm run typecheck`：通过。
- `npm run lint`：通过。
- `npm run build`：Next.js 16.2.10 生产构建通过，页面/API、`robots.txt` 与 `sitemap.xml` 均成功生成。
- `npm audit`：生产与完整依赖均为 0 个已知漏洞。

## 运行时验收

- Web 健康检查：`{"status":"ok","executionService":"ready"}`。
- 第 1 章详情能读取 Markdown，且公开响应不包含 `expectedOutput`。
- 未知练习返回 `404 / EXERCISE_NOT_FOUND`。
- 第 1 章正确答案通过可选 stdin 进入 `python:3.12-alpine` 一次性容器，输出 `我准备好了`，API 返回 `passed`。
- 同一运行时分别验证了 `passed`、`failed`、`error` 和 `timeout`；无限循环在 10,000ms 被终止。
- 真实执行首次发现 Docker 未保持 stdin 开放；补充失败回归测试后加入 `--interactive`，测试与运行时复验均通过。
- 第 5 章改为 `battle_result(boss_hp) -> str` 函数契约；3 组用例仅保存在服务端，执行响应只返回通过数量，不返回隐藏输入、期望值或运行输出。
- 第 5 章 Boss 页面以 3 HP 对应 3 组隐藏测试，全部通过后复用章节完成逻辑一次性结算 700 EXP、550 金币与「条件判断克星」称号。
- 第 6、7、9、10、11、12、14、15、17 章已迁移到同一函数契约；共 10 个 Boss、30 组私有测试，公开章节数据只包含测试数量。
- 9 个新增 Boss 均通过真实 API → 执行服务 → Docker 链路验证，每章正确实现返回 `3/3 passed`；合计实际运行 27 个隔离测试容器。
- 容器强化复验：运行用户为 `65534:65534`，禁用 IPC，内存/交换上限一致，`nofile=64`，只读根文件系统下仅 `/tmp` 可写；真实容器成功写入并读取 `/tmp`。
- 存档 v1/v2 均可迁移到严格 v3 合同；Boss 掉落、提示药水消耗、称号解锁与重复结算均由领域测试锁定。
- AI 魔法书服务严格拒绝未知字段、空问题、过大上下文和章节/练习错配；由服务端派生辅导/协作模式，Fake Provider 仅用于本地确定性验收。

## 视觉验收说明

- 修改前基线已在应用内浏览器完成 1440px 首页和 1024px 角色创建/地图检查：1024px 下无水平滚动、导航保持单行、角色创建可完成、17 个地图节点全部存在。
- 基线检查发现首页使用纯色块假预览、地图直接跳转章节、节点与装备使用字母占位；实现随后改为原创像素素材、语义化路线预览，以及“节点 → 原生任务对话框 → 开始任务”的合同流程。
- 修改后重试已完成最终浏览器验收：1440px 首页无水平溢出且像素素材全部加载；1024px 地图导航高 56px、17 个节点与图标全部渲染、页面无水平溢出。
- “节点 → 任务卡 → 开始任务”流程可完成。复验首次发现 Escape 未关闭原生对话框，补充显式键盘处理后确认 Escape 可关闭任务卡并把焦点还给原节点。
- 第 1 章可从任务卡进入，1024px 下实测双栏宽度约为 410/566px，符合 42/58；Monaco、运行按钮和结果面板均正常加载。200% 浏览器缩放仍保留为人工复核项。
- 第 2 章叙事播放器实测以全页面遮罩和像素 GUI 外框显示「第二章：Python 安装与第一个程序」：1024×768 时卡片中心为 `(512, 384)`，390×844 时为 `(195, 422)`，均与视口中心完全一致；桌面标题单行、移动端两行，进入/退出与跳过操作正常。固定舞台、逐字补全/继续、友善 NPC 发言与滚动信息流均正常。普通书名「冒险者名册」首次被误判为 NPC 发言，收紧合同并增加回归测试后已保持旁白。
- 叙事节奏复验确认旁白/对话/回顾逐字显示，补全后立即恢复为标准 Markdown；标题、列表、代码和表格教学卡无需等待、进入信息流即完整显示。11 章中的 13 个 Markdown 表格均被识别为独立教学卡。
- 17 章均达到每章至少 3 个对话节拍、2 类非旁白角色声音，逐字消息不超过 240 个纯文本字符；引用 Prompt 与提示改为即时教学卡。第 2 章「你愣了一下」和第 3 章「『区别在哪？』你问」均显示为英雄，NPC「对你说」仍保持 NPC 归类。
- 「显示全部」可直接到达本章回顾；回顾同时存在于信息流和中央回顾层，已通关状态下两处均提供「前往下一章」。第 10 章敌对消息显示「背包魔王」标签和红色语义边框。
- 第 5 章在真实 Docker 执行链路中验证：错误实现通过 2/3 组测试后 Boss 剩余 1 HP；正确实现通过 3/3 后 Boss 归零，并结算 700 EXP、550 金币与称号。
- 合法完成第 1–4 章后复验奖励：首次击败后角色为 LV4、1050 金币；再次提交正确解法后金币仍为 1050，确认幂等。未解锁章节直达时运行按钮保持禁用。
- 第 5 章在 390×844 视口下无水平溢出，Boss 与奖励面板宽 330px，三项奖励自动改为单列；浏览器控制台无应用错误。
- 第 6 章无称号奖励时两项奖励自动平分可用宽度；第 17 章在 390×844 下显示完整 dataclass 契约、三项奖励单列且无水平溢出，控制台无应用错误。
- 第 6 章移动端专注模式复验：课程/代码双标签互斥显示；Boss 上下键切换、Enter 确认、错误/正确回复均有效；失败运行后出现提示药水，消耗后背包数量从 2 降至 1、解锁提示从 0/3 变为 1/3。
- 390×844 最终复验 `scrollWidth === innerWidth === 390`，浏览器 warning/error 日志为空。
- 新叙事播放器在 390×844 下实测舞台约 226px、消息区约 438px，`scrollWidth === innerWidth === 390`；课程/代码双标签继续互斥显示，浏览器 warning/error 日志为空。
- 第 1 章表格教学卡在 390×844 下实测宽 305px，页面保持 `scrollWidth === innerWidth === 390`；中央回顾层和信息流末尾的「前往下一章」入口均存在，浏览器 warning/error 日志为空。
- 第 2 章引用提示卡在 390×844 初次复验被内容最小宽度撑到 459px；收紧消息网格后复验为 338px，位于 362px 信息流内，页面继续保持 `scrollWidth === innerWidth === 390`，浏览器 warning/error 日志为空。
- 第 2 章 AI 魔法书在真实浏览器中完成协作模式验收：推荐问题进入流式加载态，Fake Provider 回应完成后本地成功计数从 0 增至 1；390×844 下抽屉宽度为 390px、页面 `scrollWidth === innerWidth === 390`。首次回归发现 Escape 未关闭抽屉，加入显式键盘处理后确认关闭并将焦点还给入口；浏览器 warning/error 日志为空。
- 角色页生成 1200×630 像素分享卡；为保留浏览器用户手势，生成与系统分享拆为两个明确步骤，不支持 Web Share 时下载 PNG。

## 仓库卫生

- 发现上一版误将 `node_modules/` 纳入 Git，且其中 ESLint 10 / TypeScript 6 与 `package.json`、锁文件不一致。
- 已按现有 `.gitignore` 从版本控制移除依赖目录；本地依赖通过 `npm ci` 按锁文件恢复，完整审计为 0 个已知漏洞。

## 未冒充完成的外部边界

- Phase 3：AI 真实供应商与 API 凭据、可信服务端进度、额度/提示药水计费和隐私策略；账号/数据库、多设备同步、真实排行榜与公会。
- 部署：代码执行服务的平台、认证、限流、监控、扩缩容，以及是否增加 nsjail 纵深防御。
