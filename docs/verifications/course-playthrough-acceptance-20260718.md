# 全课程可通关验收

**日期**：2026-07-18
**范围**：17 章故事门禁、练习上下文、正式评测、存档恢复与代表性 GUI 路径
**结论**：通过

## 自动通关结果

| 验收层 | 结果 |
| --- | --- |
| 故事检查点 | 17 章、69 个检查点全部按顺序阻塞并可推进到回顾 |
| 正式终局 | 17/17 章通过真实执行 API |
| 评测用例 | 37/37 个公开或隐藏用例通过隔离 Python 3.12 容器 |
| 存档 | v4 往返一致，v1-v3 自动迁移，非法与重复检查点被拒绝 |
| 回归测试 | 82/82 通过；类型检查与 lint 通过 |

## 浏览器抽验

1. 第 6 章在剧情到达检查点前保持运行锁定；到达练习一后切换为 `practice`，Boss 保持 `3/3 HP`、隐藏测试保持 `0/3`。
2. 练习一自动载入文档中的 `range(1, 9)` 示例，不再显示无关的 Boss 终局模板。
3. 完成练习一并刷新后，页面恢复到练习二；右侧自动载入练习二的战利品模板，证明检查点已写入本地存档。
4. 已通关的第 5 章重新运行正式 Boss 挑战，三组隐藏测试全部通过，奖励保持只结算一次。
5. 第 1 章的 `confirm` 门禁和第 2/10 章的 `error` 门禁由领域状态机覆盖；第 2 章沿用用户已完成的人工验收结果。

## 验收中修复

- 将检查点进度从组件内存迁入版本化游戏存档，刷新不再丢失当前章实践进度。
- 从练习小节提取最近的 Python 代码块作为对应检查点起始代码；无代码块时不覆盖学员输入，正式终局恢复评测模板。
- 将 `output` 类型反馈限定为“已产生输出并由学员自查”，不再声称开放式练习已被系统精确判定。

## 边界

过程练习用于观察、试错和自查，不公开或复用终局隐藏评测；只有最后的 `pass` 节点决定章节完成、奖励与下一章解锁。
