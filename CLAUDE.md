# Project — Harness Rules

<!-- Auto-generated by Harness Automation System -->

## Harness 约束体系

本文档由 Harness Automation System 自动生成和维护，列出项目应遵守的开发约束。
约束按强度分为认知层规则（需主动判断）、软约束（linter 提醒）和参考规则（自动执行）。

## 认知层规则 (Cognitive Rules)

以下规则需要您在开发过程中主动判断和遵守：

### prefer-early-return

- **规则**: 规则不可完全形式化，需要认知层支持
- **认知层辅助**: 触发 "diagnostic" Skill
- **认知层辅助**: 触发 "educational" Skill

### no-duplicate-code

- **规则**: 规则不可完全形式化，需要认知层支持
- **认知层辅助**: 触发 "diagnostic" Skill
- **认知层辅助**: 触发 "educational" Skill

### error-handling

- **规则**: 规则不可完全形式化，需要认知层支持
- **认知层辅助**: 触发 "diagnostic" Skill
- **认知层辅助**: 触发 "educational" Skill

## 指引规则 (Guidelines)

以下规则为开发指引，建议在开发过程中参考：

- **code-review-required**: 规则不可完全形式化，需要认知层支持; 实施成本低 (2/5); 触发频率低 (2/5)

## 软约束规则 (Soft Constraints)

以下规则通过 linter 警告提醒，您在理解背景后可酌情跳过：

- **no-console-log**: 规则可形式化，适合自动化检查; 实施成本低 (1/5); 触发频率低 (2/5)
- **no-direct-fetch**: 规则可形式化，适合自动化检查; 实施成本低 (1/5); 触发频率低 (2/5)
- **type-annotations**: 规则可形式化，适合自动化检查; 实施成本低 (1/5); 触发频率低 (2/5)
- **no-magic-numbers**: 规则可形式化，适合自动化检查; 实施成本低 (1/5); 触发频率低 (2/5)
- **no-large-files**: 规则可形式化，适合自动化检查; 实施成本低 (1/5); 触发频率低 (1/5)
- **consistent-naming**: 规则可形式化，适合自动化检查; 实施成本低 (1/5); 触发频率低 (2/5)
- **no-debugger**: 规则可形式化，适合自动化检查; 实施成本低 (1/5); 触发频率低 (1/5)

## 参考规则 (Reference)

以下规则已通过其他介质自动执行：

- **commit-message-convention** → 由 `hook` 自动执行
- **test-before-merge** → 由 `ci` 自动执行
- **lint-before-commit** → 由 `hook` 自动执行
- **dependency-lock** → 由 `ci` 自动执行
- **secure-env-vars** → 由 `linter_error` 自动执行

---

_Generated by Harness Automation System v1.0_
